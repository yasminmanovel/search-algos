// need to be able to access the readData functions

list calculatePageRank(graph g, float d, float diffPR, int maxIterations);
list order(list UrlsPageRanks);
void printOrderedList(list orderedList)
